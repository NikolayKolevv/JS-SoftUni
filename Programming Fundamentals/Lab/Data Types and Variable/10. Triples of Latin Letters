function triples(n) {
    let firstLetter = 'a'.charCodeAt(0);
  
    for (let a = 0; a < n; a++) {
      for (let b = 0; b < n; b++) {
        for (let c = 0; c < n; c++) {
          console.log(String.fromCharCode(firstLetter + a, firstLetter + b, firstLetter + c));
        }
      }
    }
  }
